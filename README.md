# Historique des mesures de performance

Branche écrite par le job `benchmarks` de `.github/workflows/nightly.yml` (github-action-benchmark) : données dans `dev/bench/data.js`, graphique dans `dev/bench/index.html`. Ne pas y commiter à la main.
