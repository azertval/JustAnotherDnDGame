{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://justanotherdndgame.local/rpg/feat.schema.json",
  "title": "Don",
  "description": "Un des quarante-deux dons. Le nom fait autorite depuis le LEXIQUE (LOT-30) et non depuis le livre : les titres de dons du Manuel des Joueurs sont des scans mutiles, et les deux sources francaises ne traduisent pas pareil -- << Adepte des elements >> contre << Adepte elementaire >>. `variantes` conserve la graphie du livre pour que la correspondance reste tracable.",
  "type": "object",
  "properties": {
    "id": true,
    "name": true,
    "source": true,
    "status": true,
    "asset": true,
    "mecanismesRequis": true,
    "narratif": true,
    "prerequisite": {
      "type": "string",
      "minLength": 2,
      "description": "Condition d'acces, telle que le livre l'enonce (EX-RPG-040). Absente si le don n'en a pas."
    },
    "variantes": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true,
      "description": "Autres traductions attestees du meme don. Le champ `name` fait foi ; celles-ci servent a retrouver l'entree sous le nom qu'un autre document lui donne."
    },
    "text": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "id",
    "name",
    "source"
  ],
  "additionalProperties": false,
  "allOf": [
    {
      "$ref": "https://justanotherdndgame.local/rpg/common.schema.json#/$defs/entree"
    }
  ]
}
