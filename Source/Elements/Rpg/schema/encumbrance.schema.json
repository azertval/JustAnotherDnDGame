{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://justanotherdndgame.local/rpg/encumbrance.schema.json",
  "title": "Capacite de charge et encombrement",
  "description": "Ce qu'un personnage peut porter, et ce qui lui arrive quand il porte trop. Toutes les bornes sont exprimees en GRAMMES PAR POINT DE FORCE, et non en kilogrammes : le corpus ecrit << votre valeur de Force multipliee par 7,5 >> en kg, mais les poids d'objets sont deja en grammes entiers dans les catalogues (LOT-34), et melanger les deux unites dans une somme est exactement l'erreur qu'une unite unique supprime. EX-VIS-007 interdit que ces nombres vivent dans le C++ : un `7500` nu dans un calcul de charge ne dit pas ce qu'il represente. Chacun porte la PHRASE du livre qui l'atteste.",
  "allOf": [
    {
      "$ref": "https://justanotherdndgame.local/rpg/common.schema.json#/$defs/entree"
    }
  ],
  "type": "object",
  "properties": {
    "id": true,
    "name": true,
    "source": true,
    "status": true,
    "asset": true,
    "mecanismesRequis": true,
    "narratif": true,
    "carryingCapacityGramsPerStrength": {
      "type": "integer",
      "minimum": 1,
      "description": "Capacite de charge, en grammes par point de Force. Au-dela, le personnage ne porte plus : c'est le plafond, pas un seuil de penalite."
    },
    "encumberedGramsPerStrength": {
      "type": "integer",
      "minimum": 1,
      "description": "Seuil d'encombrement, en grammes par point de Force (variante d'encombrement du livre)."
    },
    "heavilyEncumberedGramsPerStrength": {
      "type": "integer",
      "minimum": 1,
      "description": "Seuil d'encombrement lourd, en grammes par point de Force."
    },
    "encumberedSpeedPenaltyMeters": {
      "type": "number",
      "minimum": 0,
      "description": "Vitesse retiree, en metres, au personnage encombre."
    },
    "heavilyEncumberedSpeedPenaltyMeters": {
      "type": "number",
      "minimum": 0,
      "description": "Vitesse retiree, en metres, au personnage lourdement encombre."
    },
    "attestations": {
      "type": "object",
      "description": "Pour chaque constante, la page et la phrase du corpus qui la portent. Une constante extraite sans son attestation est une constante tapee de memoire.",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "page": {
            "type": "integer",
            "minimum": 1
          },
          "text": {
            "type": "string",
            "minLength": 10
          }
        },
        "required": ["page", "text"],
        "additionalProperties": false
      }
    }
  },
  "required": [
    "id",
    "name",
    "source",
    "carryingCapacityGramsPerStrength",
    "encumberedGramsPerStrength",
    "heavilyEncumberedGramsPerStrength",
    "encumberedSpeedPenaltyMeters",
    "heavilyEncumberedSpeedPenaltyMeters",
    "attestations"
  ],
  "additionalProperties": false
}
